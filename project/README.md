# Product Manager (React + Vite + Bootstrap)

A simple demo project demonstrating React functional components, props, state,
controlled inputs, and conditional rendering. Uses Bootstrap for styling.

## How to run

1. Install dependencies:
   npm install

2. Run dev server:
   npm run dev

Open the URL shown by Vite (usually http://localhost:5173).
